# Nix
result
result-*
.direnv/

# luarocks / lux build artifacts
.luarocks/
lua_modules/
*.rock
*.src.rock

# Neovim helptags (generated from doc/*.txt)
doc/tags

# Demo recorder scratch output (frames captured by scripts/record-demo.sh)
scratch_frames/

# OS cruft
.DS_Store

# Local secrets (e.g. LUAROCKS_API_KEY for `just publish`)
.env

# Dev git worktrees (parallel-agent scratch)
.worktrees/

# Generated by the flake devshell (source of truth: flake/pre-commit.nix)
.pre-commit-config.yaml
